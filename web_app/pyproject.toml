[project]
name = "gemelo-digital-web"
version = "0.3.7"
description = "Edicion web serverless del gemelo digital del mini horno solar"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "shiny>=1.6.3",
    "shinylive>=0.8.9",
]

[tool.uv]
package = false
